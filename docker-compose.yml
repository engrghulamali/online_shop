version: "3.8"

services:
  online-shop:
    container_name: online-shop-container
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    command: ["npm", "run", "dev"]
    environment:
      - NODE_ENV=development
